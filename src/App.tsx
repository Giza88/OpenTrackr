// React App component removed — project now uses `app.js` at project root.

export default function AppPlaceholder() {
  // placeholder to avoid build errors in tools that still reference this file
  return null;
}
